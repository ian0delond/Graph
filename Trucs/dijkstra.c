#include <limits.h>

void Dijkstra(graphe_matrice *g, int s)
{
	int u;
	int i;
	int distance[g->taille];
	int pere[g->taille];

	for (u=0;u<g->taille;u++)
	{
		distance[u]=INT_MAX;
		pere[u]=u;
	}

}

int main (){

return 0;
}
